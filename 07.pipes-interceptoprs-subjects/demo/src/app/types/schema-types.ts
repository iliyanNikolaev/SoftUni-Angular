export type User = {
    email: string,
    password: string,
    firstName: string,
    lastName: string,
    profilePic: string,
    posts: any[],
    connections: any[],
    _id: string
}

export type Post = {
    
}